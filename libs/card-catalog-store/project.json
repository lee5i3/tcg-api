{
  "name": "card-catalog-store",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "cwd": "libs/card-catalog-store", "command": "go build ./..." }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "cwd": "libs/card-catalog-store", "command": "go test ./..." }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/card-catalog-store",
        "commands": ["go vet ./...", "test -z \"$(gofmt -l .)\" || (gofmt -l . && exit 1)"]
      }
    }
  }
}
