{
  "name": "httpapi",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "implicitDependencies": ["card-catalog-store"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "cwd": "libs/httpapi", "command": "go build ./..." }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "cwd": "libs/httpapi", "command": "go test ./..." }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/httpapi",
        "commands": ["go vet ./...", "test -z \"$(gofmt -l .)\" || (gofmt -l . && exit 1)"]
      }
    }
  }
}
