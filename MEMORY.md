# MEMORY.md — Claude request log

A running log of what was asked of Claude in this repo and what changed.
Newest first. Keep entries short; details live in `docs/decisions/`.

| Date | Request | Outcome |
| --- | --- | --- |
| 2026-08-14 | Add social logins (Facebook, Google, Apple) to the app login page | Full OAuth authorization-code flow in `api-auth-routes` (`/v1/auth/providers` + `oauth/{provider}/start\|callback`, signed state JWTs, Apple form_post); email-based account linking via `FindOrCreateOAuthUser` (`USERIDENT#` guard items, social-only accounts have no password); buttons render only for configured providers; terraform vars (`app_url` + 3× id/secret), compose passthrough env. ADR 0008 |
| 2026-08-14 | Admin login should match the app | Admin `/login` rebuilt on the same `AuthSplit` dark split-screen: brand lockup + Admin chip, dark icon-inset token field, full-width accent button; right panel "Run the catalog." with a catalog-at-a-glance stat card; admin layout renders `/login` bare (no SiteHeader). Token flow/tests unchanged |
| 2026-08-14 | Login page laid out like a supplied dark split-screen SaaS reference | `/login` + `/register` rebuilt as full-viewport dark split: left form column (brand lockup, icon-inset fields, accent button, "or" divider, outlined alternate action; no dead "Forgot?" link), right gradient panel with tagline + mini live-prices stat card (green/red deltas); shared `AuthSplit` shell added to `@tcg/design-system`; right panel hides <900px |
| 2026-08-14 | Move sites into `apps/web/`; rename marketing → **www**; redesign — owner dislikes the packaging look AND the oval logo; new inspiration: **TCGplayer, Collectr, RareCandy** (modern marketplace/portfolio aesthetic) | `apps/web/{app,admin,www}` (NX names app/admin/www, `@tcg/www`); compose service + terraform `www_site`/`www_*` outputs renamed; design system rebuilt: clean white/dark-navy surfaces, saturated modern accent, Inter-led type (no uppercase/condensed), new minimal stacked-cards logo mark replacing the retro oval |
| 2026-08-14 | Marketing, app, and admin must share the same look & feel; share via an npm lib. Owner supplied a reference image (vintage card-supply packaging) for the login page — brand follows it | New `libs/design-system` (`@tcg/design-system`): tokens.css + base.css + shared Svelte components; ONE brand across all sites built from the reference: **navy panels + cream background + red tag accent + condensed uppercase display type + retro oval badge**; app login/register styled as a "packaging label" (benefit bullets, red tag secondary CTA). Admin identified by a header badge, not its own theme |
| 2026-08-14 | App must start with a login page that also offers registration | Real user accounts: `libs/user-accounts` (bcrypt, email-guard uniqueness in the single table), new λ `api-auth-routes` (`/v1/auth/register\|login\|me` + admin `check` moved here; 7-day HS256 JWTs, `USER_JWT_SECRET`), `httpapi.Dispatch` learned public routes; app gates all routes behind `/login`+`/register` (token in localStorage). Catalog reads stay public API surface. ADR 0007 |
| 2026-08-14 | Marketing site must have an env var saying where the app is | `PUBLIC_APP_URL` wired as a **build arg** (site is prerendered → baked into CTAs): marketing Dockerfile ARG/ENV, compose passes `${PUBLIC_APP_URL:-http://localhost:8080}`, prod deploy documented as `PUBLIC_APP_URL="$(terraform output -raw app_url)" npx nx build marketing` |
| 2026-08-14 | 404 on :8080 (`make dev`) | Two causes: the pre-conversion `tcg-api` + `tcg-api-postgres` containers were still running (17h) and the old Go service held host :8080 (stopped/removed; postgres volume kept); and app/admin nginx resolved `gateway` eagerly at startup, crash-looping on a race (templates now resolve lazily via Docker DNS `resolver 127.0.0.11` + variable `proxy_pass`; compose services got `restart: unless-stopped`). Verified end-to-end: all sites 200, same-origin /v1 proxy + auth check working |
| 2026-08-14 | Terraform is production-only — drop the environment concept; add a Makefile with `make dev` | Environment variable/suffixes removed from all four stacks (names like `tcg-catalog`, `tcg-api-card-routes`); production defaults (deletion protection on, no ECR force-delete); per-stack `terraform.tfvars.example`; push-images.sh/build.yaml/infra.yaml env-less; Makefile (`make dev` = compose stack, plus test/lint/build/images/down) |
| 2026-08-13 | Sites → **SvelteKit static**; new **marketing** site (pricing/screenshots/CTA→app); drop Python (Node + Go only); rename compose to `.yaml`; dependabot; CI split into `test`/`checks-nodejs`/`checks-golang`/`build`/`infra` workflows as PR gates; libs named specifically (`catalog` → `card-catalog-store`); Terraform split into independent stacks; CLAUDE.md keep-updated rule | apps/{marketing,app,admin} in SvelteKit (dist/ static); tools/importer deleted; `libs/card-catalog-store`; `infra/terraform/stacks/{database,api,jobs,sites}` wired by remote state; `.github/` workflow suite + dependabot.yaml. ADR 0006 |
| 2026-08-13 | Split web into public **app** + separate **admin** (limited logins); Lambdas get specific function names (`api-card-routes`, `pokemon-price-updater`) and deploy as **Docker images**; flatten `libs/` (no per-language folders); create this MEMORY.md; docker-compose runs the whole site locally | `apps/app` + `apps/admin` (token login via new `POST /v1/auth/check`); lambdas renamed + Dockerfiles + ECR/image Terraform; new `pokemon-price-updater` scheduled job (+ variant price write path in the store); `libs/{catalog,httpapi,api-client}`; `tools/local-gateway` + full compose stack. ADR 0005 |
| 2026-08-13 | API split into one project per Lambda; CloudFront multi-origin for web; shared code only in named libs | Per-Lambda NX projects/Go modules; `libs/go/httpapi`, `libs/ts/api-client` extracted; CloudFront S3 + API origins (same-origin API). ADR 0004 |
| 2026-08-13 | Convert PostgreSQL→DynamoDB, API→Lambdas, repo→NX monorepo (Node/Go/Python by fit), Terraform infra, tests, docs, CLAUDE.md | Initial monorepo: Go Lambdas + single-table DynamoDB, React site, Python importer, Terraform, docs + ADR 0001–0003 |

## Conventions for this file

- One row per user request (not per commit).
- Record the *ask* and the *shape of the outcome*, with a pointer to the ADR
  when one exists.
- Claude: append a row here whenever a session materially changes the repo.
