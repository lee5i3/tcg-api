# Absolute URL of the public catalog app. Every CTA on the marketing site
# links here (registration/payment does not exist yet, so "Get started"
# simply opens the app).
#
# The value is baked into the prerendered pages at build time.
# Defaults to the placeholder below when unset.
PUBLIC_APP_URL=https://app.example.com
